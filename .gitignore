.omx/

# node-ssh-bootstrap: 每环境的真实参数(SSH_HOST/KUBECTL/...)只在本地,
# env.example 入库做模板
node-ssh-bootstrap/env.*
!node-ssh-bootstrap/env.example

# 各机本地密文目录(token/kubeconfig);绝不入库
envs/

# Python bytecode generated while running skill helper scripts.
__pycache__/
*.py[cod]
